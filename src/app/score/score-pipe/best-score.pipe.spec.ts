import { BestScorePipe } from './best-score.pipe';

describe('BestScorePipe', () => {
  it('create an instance', () => {
    const pipe = new BestScorePipe();
    expect(pipe).toBeTruthy();
  });
});
